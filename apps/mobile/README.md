# mobile

BenchFinder Expo app. CNG workflow: `android/` and `ios/` are generated by `expo prebuild` and never committed.

```bash
pnpm android:build        # one-time native dev client build (runs Gradle)
pnpm android              # daily loop: starts Metro and opens installed dev client
pnpm start                # Metro only (manual app launch)
pnpm prebuild             # regenerate native projects when native config changes
eas build --profile development --platform android
```

Routes live in `app/` (expo-router). Feature code lives in `src/features/` as vertical slices. See `BENCHFINDER_ARCHITECTURE.md` at the repo root, section 8.
