packages:
  - apps/*
  - packages/*

allowBuilds:
  unrs-resolver: true

# No web target: expo-router's web-only transitive deps (radix, vaul) want
# react-dom, which native builds never load.
peerDependencyRules:
  ignoreMissing:
    - react-dom
